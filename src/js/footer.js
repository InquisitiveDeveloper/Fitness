import React from "react";
import { Grid, Row, Col } from "react-flexbox-grid";

const Footer = () => {
  return (
    
    <Row center="xs">
        <Col xs={6}>This is a footer component</Col>
  </Row>
  );
};

export default Footer;
